#text = input("Tell me something and i will rewrite it for you correctly: ")
#replace = text.replace(' ','...')


#print(replace)

def main():
    text=input("Write something and I will slow it down: ")
    #slowdown=text.replace(" ","...")
    print(slowdown(text))

def slowdown(text)
    return text=text.replace(" ","...")
main()
